# Проверка задачи 001 — круг 1: вернуть на доработку

Проверено: 18 тестов проходят; на настоящих ячейках скрипт работает
(старая ячейка Muse с 325 пустыми ответами из 388 поймана). Сводка годного
файла не изменилась.

## Замечания

1. **Падение на корректном JSON неверной формы.** Файл с содержимым `[1, 2]`
   даёт `AttributeError: 'list' object has no attribute 'get'`. Требование
   задачи — «проблема, а не падение скрипта». Нужно: если корень не объект,
   `outcomes` не список, элемент `outcomes` не объект или `summary` /
   `чем сделано` не объект — вернуть проблему «неверная структура файла: …»
   и продолжить со следующим файлом. Добавить тесты на эти случаи.
2. **ruff:** `tests/test_inspect_cell.py:3:1: I001` — порядок импортов.
   Проверка: `C:\python\rag_textbook\.venv\Scripts\python.exe -m ruff check
   scripts/inspect_cell.py tests/test_inspect_cell.py` должна быть чистой.
3. Временную папку `.pytest_cache/tmp-001-run1` я удалил. Если снова нужен
   `--basetemp`, после прогона удаляй её сам и упомяни в отчёте.

Отчёт дополни разделом «Круг 2» в `tasks/001-report.md`.

# Круг 2: ПРИНЯТО

Проверено исследователем независимо от отчёта:
- `tests/test_inspect_cell.py` — 31 passed; весь набор — 448 passed;
- ruff чистый;
- `[1, 2]` и `{"outcomes":[1],"summary":[]}` бракуются с кодом 1, без падения;
- временную папку `.pytest_cache/tmp-001-run2` удалил я.

Оставшееся ограничение (нестроковые `answer` / `модель ответа`) принимается:
такие файлы наш замер не пишет. Коммит — за человеком.
