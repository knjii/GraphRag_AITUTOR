# Задача 006: проверка источников и новизны для docs/RESEARCH-2026-09.md

## Зачем

Исследователь свёл обзор в `docs/RESEARCH-2026-09.md`. Перед тем как
объявлять исследовательский компонент, нужно (а) убедиться, что ссылки и
цифры верны, (б) **попытаться опровергнуть заявленную новизну** — найти
работы, которые уже сделали то же самое.

Твои скачанные материалы лежат в `%TEMP%\lit003` и `%TEMP%\research004` —
используй их, не качай заново.

## Что сделать

1. **Сверка ссылок.** Для каждого arXiv ID из раздела 8 документа проверь,
   что название соответствует упомянутой работе. Для каждой цифры в
   таблицах разделов 2.2 и 2.3 — что она есть в указанной работе и таблице.
   Результат — таблица «утверждение → верно / неверно / не нашёл → где».
2. **Поиск опровержений новизны (главное).** Раздел 2.4 утверждает, что
   пусто: (1) RL-награда за перенос математических объектов/формул в
   RAG-ответе; (2) RL для русскоязычного учебного math-RAG;
   (3) обучение отбора/реранкинга поднимать находки графа; (4) контроли
   ложной награды в RL-RAG. Ищи целенаправленно (2024–2026):
   faithfulness/grounding rewards для RAG (RLVR, GRPO), context utilization
   RL, «copy/extract» rewards, formula/LaTeX-aware generation, math QA over
   textbooks с RL, citation/attribution RL (продолжатели 2402.04315),
   Russian RAG RL. Для каждой близкой работы — насколько она перекрывает
   наш замысел (полностью / частично / нет) и чем отличается.
3. **Списковое RL-реранжирование** для компонента №2: работы, где LLM-
   реранкер или отборщик контекста обучается RL (например, Rank-R1,
   REARANK и аналоги — проверь, существуют ли), постановка, награда,
   размер модели, результаты.
4. **Малые модели и память.** Подтверди или опровергни по первоисточникам:
   поддержка Qwen3.5 в vLLM и в Unsloth GRPO (`fast_inference`), реальные
   замеры GRPO+LoRA для 4B на 24 ГБ (кто, какие скорости).

## Формат результата

`tasks/006-report.md`, пиши **по ходу**: разделы 1–4, в конце вывод
«новизна подтверждена / частично / опровергнута» по каждому из четырёх
пунктов с аргументами и ссылками.

## Можно менять

- `tasks/006-report.md` (новый)

## Критерий приёмки

Каждое утверждение документа проверено; по новизне — не меньше 10
просмотренных близких работ со ссылками; выборочная проверка сходится.
