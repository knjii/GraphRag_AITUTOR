"""RAG-ассистент для глубокого изучения математической и технической литературы.

Пакет собран из четырёх слоёв, каждый из которых можно тестировать отдельно:

* ``parsing`` / ``chunking`` — превращение PDF в чанки с сохранением формул и таблиц;
* ``models`` — доменные типы, общие для всех слоёв;
* ``stores`` — Qdrant (векторы) и Neo4j (граф знаний);
* ``retrieval`` / ``generation`` — конвейер ответа на вопрос.
"""

__version__ = "0.2.0"
